apiVersion: v1
kind: Service
metadata:
  name: simple-shop
  namespace: devsecops
  labels:
    app: simple-shop
spec:
  type: NodePort
  selector:
    app: simple-shop
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
      name: http

apiVersion: v1
kind: Service
metadata:
  name: security-dashboard
  namespace: devsecops
  labels:
    app: security-dashboard
spec:
  type: NodePort
  selector:
    app: security-dashboard
  ports:
    - name: http
      port: 4100
      targetPort: 4100
      nodePort: 30410
      protocol: TCP
